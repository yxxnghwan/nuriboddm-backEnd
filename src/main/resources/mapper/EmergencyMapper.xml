<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTDMapper3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="com.nuribodeum.mapper.EmergencyMapper">
		<insert id="insertEmergency" parameterType="com.nuribodeum.vo.EmergencyVO">
			INSERT INTO EMERGENCY(latitude, longitude, heart_rate, user_id)
			VALUES( #{latitude}, #{longitude}, #{heart_rate}, #{user_id})
		</insert>
	</mapper>